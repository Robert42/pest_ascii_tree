root = _{ SOI ~ expr ~ EOI }
expr_root = _{ val ~ (op ~ leaf)* }

expr = { val ~ (op ~ leaf)* }

leaf = _{ "(" ~ expr ~ ")" | val }
op = { "+" | "-" }

val = { ASCII_DIGIT+ | ASCII_ALPHA+ }

WHITESPACE = _{ " " }
